# Деплой на Яндекс Облако

Если у вас уже есть виртуальная машина в Яндекс Облаке - отлично! Это даст вам полный контроль над сервером.

## 📋 Требования

- Виртуальная машина в Яндекс Облаке (Ubuntu 20.04/22.04 или Debian)
- Минимум 1 GB RAM, 1 vCPU
- Публичный IP адрес
- SSH доступ к серверу

## 🚀 Быстрая установка

### Шаг 1: Подключитесь к серверу

```bash
ssh username@ваш-публичный-ip
```

### Шаг 2: Установите Nginx и Certbot

```bash
# Обновите пакеты
sudo apt update
sudo apt upgrade -y

# Установите Nginx
sudo apt install nginx -y

# Установите Certbot для SSL
sudo apt install certbot python3-certbot-nginx -y

# Проверьте что Nginx запущен
sudo systemctl status nginx
```

### Шаг 3: Настройте файрвол

```bash
# Разрешите HTTP и HTTPS
sudo ufw allow 'Nginx Full'
sudo ufw allow OpenSSH
sudo ufw enable
```

### Шаг 4: Создайте директорию для сайта

```bash
# Создайте папку для сайта
sudo mkdir -p /var/www/defendend.dev

# Дайте права вашему пользователю
sudo chown -R $USER:$USER /var/www/defendend.dev
sudo chmod -R 755 /var/www/defendend.dev
```

### Шаг 5: Загрузите собранный проект

На **вашем локальном компьютере**:

```bash
# Соберите production версию
./gradlew jsBrowserProductionWebpack

# Скопируйте файлы на сервер
scp -r build/dist/js/productionExecutable/* username@ваш-публичный-ip:/var/www/defendend.dev/
```

Или через Git (более удобно для обновлений):

```bash
# На сервере
cd /var/www/defendend.dev
git clone https://github.com/ваш-username/Ai.git temp
cd temp
./gradlew jsBrowserProductionWebpack
cp -r build/dist/js/productionExecutable/* ../
cd ..
rm -rf temp
```

### Шаг 6: Настройте Nginx

```bash
# Создайте конфиг для сайта
sudo nano /etc/nginx/sites-available/defendend.dev
```

Вставьте следующий конфиг:

```nginx
server {
    listen 80;
    listen [::]:80;

    server_name defendend.dev www.defendend.dev;

    root /var/www/defendend.dev;
    index index.html;

    # Логи
    access_log /var/log/nginx/defendend.dev.access.log;
    error_log /var/log/nginx/defendend.dev.error.log;

    # Основная локация
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Кэширование статических файлов
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Безопасность
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Gzip сжатие
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;
}
```

Сохраните (Ctrl+O, Enter, Ctrl+X).

### Шаг 7: Активируйте конфиг

```bash
# Создайте симлинк
sudo ln -s /etc/nginx/sites-available/defendend.dev /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

### Шаг 8: Настройте DNS на Рег.ру

1. Войдите в панель Рег.ру
2. Перейдите в управление доменом defendend.dev
3. Удалите старые A записи
4. Добавьте новые:

```
Тип: A
Имя: @
Значение: ВАШ-ПУБЛИЧНЫЙ-IP-ИЗ-ЯНДЕКС-ОБЛАКА
TTL: 3600

Тип: A
Имя: www
Значение: ВАШ-ПУБЛИЧНЫЙ-IP-ИЗ-ЯНДЕКС-ОБЛАКА
TTL: 3600
```

5. Подождите 15-30 минут для распространения DNS

### Шаг 9: Установите SSL сертификат

```bash
# Получите бесплатный SSL от Let's Encrypt
sudo certbot --nginx -d defendend.dev -d www.defendend.dev
```

Следуйте инструкциям Certbot:
- Введите email для уведомлений
- Согласитесь с ToS
- Выберите опцию 2 (перенаправлять HTTP на HTTPS)

### Шаг 10: Настройте автообновление SSL

```bash
# Certbot автоматически добавляет cron job, проверьте:
sudo certbot renew --dry-run
```

Если ошибок нет - все готово!

## ✅ Проверка

Откройте в браузере: https://defendend.dev

Должен загрузиться ваш AI чат!

---

## 🔄 Автоматический деплой через GitHub Actions

Для автоматического обновления при push в GitHub:

### 1. Создайте SSH ключ для деплоя

На сервере:

```bash
# Создайте новый SSH ключ для деплоя (без пароля)
ssh-keygen -t ed25519 -C "deploy@defendend.dev" -f ~/.ssh/deploy_key

# Добавьте публичный ключ в authorized_keys
cat ~/.ssh/deploy_key.pub >> ~/.ssh/authorized_keys

# Скопируйте приватный ключ (понадобится для GitHub)
cat ~/.ssh/deploy_key
```

### 2. Добавьте секреты в GitHub

В вашем репозитории на GitHub:
- Settings → Secrets and variables → Actions → New repository secret

Создайте 3 секрета:

```
SSH_PRIVATE_KEY - приватный ключ из ~/.ssh/deploy_key
SSH_HOST - публичный IP вашего сервера
SSH_USER - ваш username на сервере
```

### 3. Создайте GitHub Actions workflow

Уже создан! Файл `.github/workflows/deploy.yml`, но нужно изменить для Яндекс Облака:

```bash
# На локальном компьютере создайте новый файл
mkdir -p .github/workflows
```

Создайте `.github/workflows/deploy-yandex.yml`:

```yaml
name: Deploy to Yandex Cloud

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Build project
        run: ./gradlew jsBrowserProductionWebpack --no-daemon

      - name: Deploy to server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "build/dist/js/productionExecutable/*"
          target: "/var/www/defendend.dev"
          strip_components: 4
          rm: true
```

### 4. Готово!

Теперь при каждом push в main ветку:
1. GitHub Actions соберет проект
2. Автоматически загрузит файлы на сервер
3. Сайт обновится

---

## 📊 Скрипт для быстрого обновления вручную

Создайте скрипт на **локальном компьютере**:

```bash
nano deploy.sh
```

```bash
#!/bin/bash

echo "Building production..."
./gradlew jsBrowserProductionWebpack

echo "Deploying to Yandex Cloud..."
scp -r build/dist/js/productionExecutable/* username@ваш-ip:/var/www/defendend.dev/

echo "Done! Site updated at https://defendend.dev"
```

Сделайте исполняемым:

```bash
chmod +x deploy.sh
```

Теперь для обновления сайта просто:

```bash
./deploy.sh
```

---

## 🔧 Полезные команды

### Просмотр логов Nginx

```bash
# Логи доступа
sudo tail -f /var/log/nginx/defendend.dev.access.log

# Логи ошибок
sudo tail -f /var/log/nginx/defendend.dev.error.log
```

### Перезапуск Nginx

```bash
sudo systemctl restart nginx
```

### Проверка статуса SSL

```bash
sudo certbot certificates
```

### Обновление SSL сертификата

```bash
sudo certbot renew
```

---

## 💡 Оптимизация

### 1. Настройте CDN Яндекс Облака (опционально)

Для ускорения загрузки можно настроить CDN:
- https://cloud.yandex.ru/docs/cdn/

### 2. Включите HTTP/2

В конфиге Nginx измените:

```nginx
listen 443 ssl http2;
listen [::]:443 ssl http2;
```

### 3. Мониторинг

Установите мониторинг состояния сервера:

```bash
sudo apt install htop -y
htop
```

---

## ⚖️ Сравнение: Яндекс Облако vs Vercel

| Параметр | Яндекс Облако | Vercel |
|----------|---------------|--------|
| **Цена** | ~500₽/мес | Бесплатно |
| **Настройка** | 30-60 минут | 5 минут |
| **Поддержка** | Сами | Автоматическая |
| **Скорость** | Зависит от региона | Глобальный CDN |
| **Автодеплой** | Нужно настроить | Из коробки |
| **SSL** | Нужно настроить | Автоматически |
| **Контроль** | Полный | Ограниченный |
| **Масштабирование** | Ручное | Автоматическое |

### Когда использовать Яндекс Облако:

✅ У вас уже есть сервер (не нужно платить дополнительно)
✅ Нужен полный контроль над сервером
✅ Планируете размещать несколько проектов на одном сервере
✅ Хотите добавить backend в будущем
✅ Нужны специфические настройки или софт

### Когда использовать Vercel:

✅ Хотите сэкономить время
✅ Не хотите заниматься администрированием
✅ Нужен только статический сайт
✅ Важна скорость загрузки по всему миру
✅ Нужен автоматический деплой

---

## 🎯 Рекомендация

Если сервер в Яндекс Облаке **уже работает** и используется для других проектов - развертывайте там! Это будет бесплатно и даст опыт работы с production сервером.

Если сервер **специально для этого проекта** - подумайте, стоит ли платить 500₽/мес, когда есть бесплатный Vercel с автодеплоем.

**Можно сделать оба варианта:**
- Production на Яндекс Облаке (defendend.dev)
- Staging на Vercel (для тестирования)

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте логи: `sudo tail -f /var/log/nginx/error.log`
2. Проверьте firewall: `sudo ufw status`
3. Проверьте DNS: `nslookup defendend.dev`
4. Проверьте SSL: `sudo certbot certificates`

Все должно работать! 🚀
