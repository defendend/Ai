# Руководство по развертыванию на defendend.dev

## Варианты деплоя

Выберите один из вариантов ниже. Рекомендую **Vercel** или **Cloudflare Pages** - проще всего.

---

## 🚀 Вариант 1: Vercel (РЕКОМЕНДУЕТСЯ)

### Шаги:

1. **Залейте код на GitHub** (если еще не сделали)
   ```bash
   git add .
   git commit -m "Ready for deployment"
   git push origin main
   ```

2. **Зарегистрируйтесь на Vercel**
   - Перейдите на https://vercel.com
   - Войдите через GitHub

3. **Импортируйте проект**
   - Нажмите "Add New Project"
   - Выберите ваш репозиторий
   - Vercel автоматически обнаружит `vercel.json`

4. **Настройте домен**
   - В настройках проекта → Domains
   - Добавьте `defendend.dev`
   - Vercel даст вам DNS записи для настройки

5. **Настройте DNS у вашего регистратора**

   Добавьте следующие записи:
   ```
   Type: A
   Name: @
   Value: 76.76.21.21

   Type: CNAME
   Name: www
   Value: cname.vercel-dns.com
   ```

6. **Готово!**
   - Vercel автоматически деплоит при каждом push
   - SSL сертификат настроится автоматически

---

## 📄 Вариант 2: GitHub Pages

### Шаги:

1. **Залейте код на GitHub**
   ```bash
   git add .
   git commit -m "Ready for deployment"
   git push origin main
   ```

2. **Включите GitHub Pages**
   - Откройте Settings → Pages в вашем репозитории
   - Source: GitHub Actions
   - Workflow уже настроен в `.github/workflows/deploy.yml`

3. **Дождитесь первого деплоя**
   - Перейдите во вкладку Actions
   - Дождитесь завершения workflow
   - Сайт будет доступен на `https://<username>.github.io/<repo-name>/`

4. **Настройте кастомный домен**
   - В Settings → Pages → Custom domain
   - Введите `defendend.dev`
   - Нажмите Save

5. **Настройте DNS**

   У вашего регистратора домена добавьте:
   ```
   Type: A
   Name: @
   Value: 185.199.108.153

   Type: A
   Name: @
   Value: 185.199.109.153

   Type: A
   Name: @
   Value: 185.199.110.153

   Type: A
   Name: @
   Value: 185.199.111.153

   Type: CNAME
   Name: www
   Value: <username>.github.io
   ```

6. **Включите HTTPS**
   - Подождите ~24 часа для DNS propagation
   - В Settings → Pages включите "Enforce HTTPS"

---

## ⚡ Вариант 3: Cloudflare Pages

### Шаги:

1. **Залейте код на GitHub**
   ```bash
   git add .
   git commit -m "Ready for deployment"
   git push origin main
   ```

2. **Зарегистрируйтесь на Cloudflare**
   - https://dash.cloudflare.com/sign-up
   - Добавьте домен `defendend.dev`
   - Следуйте инструкциям по смене nameservers

3. **Создайте Pages проект**
   - Pages → Create a project
   - Connect to Git → Выберите репозиторий
   - Build settings:
     - Build command: `./gradlew jsBrowserProductionWebpack`
     - Build output directory: `build/dist/js/productionExecutable`
     - Environment variables: `JAVA_VERSION = 17`

4. **Настройте кастомный домен**
   - В проекте → Custom domains
   - Добавьте `defendend.dev`
   - DNS настроится автоматически (т.к. домен уже в Cloudflare)

5. **Готово!**
   - Самый быстрый CDN
   - Автоматический SSL
   - Автодеплой при push

---

## 🌐 Вариант 4: Netlify

### Шаги:

1. **Залейте код на GitHub**
   ```bash
   git add .
   git commit -m "Ready for deployment"
   git push origin main
   ```

2. **Зарегистрируйтесь на Netlify**
   - https://app.netlify.com/signup
   - Войдите через GitHub

3. **Импортируйте проект**
   - Add new site → Import an existing project
   - Выберите GitHub → Выберите репозиторий
   - Netlify автоматически обнаружит `netlify.toml`
   - Deploy site

4. **Настройте домен**
   - Domain settings → Add custom domain
   - Введите `defendend.dev`

5. **Настройте DNS**

   У регистратора домена:
   ```
   Type: A
   Name: @
   Value: 75.2.60.5

   Type: CNAME
   Name: www
   Value: <your-site>.netlify.app
   ```

6. **SSL автоматически настроится**

---

## 🔧 Локальное тестирование production build

Перед деплоем можно проверить локально:

```bash
# Сборка
./gradlew jsBrowserProductionWebpack

# Запуск простого HTTP сервера
cd build/dist/js/productionExecutable
python3 -m http.server 8000

# Откройте http://localhost:8000
```

---

## 📝 Важные заметки

1. **SSL/HTTPS обязателен для .dev доменов** - все платформы настраивают автоматически

2. **API ключ**
   - Пользователи вводят свой API ключ в UI
   - Ключ хранится в localStorage браузера
   - Никогда не коммитьте API ключи в Git!

3. **CORS**
   - Anthropic API поддерживает CORS для браузерных запросов
   - Если возникнут проблемы, можно добавить прокси через Cloudflare Workers

4. **Автодеплой**
   - Все платформы автоматически деплоят при push в main
   - GitHub Actions workflow уже настроен для GitHub Pages

---

## 🆘 Troubleshooting

### Проблема: DNS не работает
- Подождите 24-48 часов для propagation
- Проверьте настройки: `nslookup defendend.dev`

### Проблема: 404 Not Found
- Убедитесь что build output directory указан правильно
- Проверьте что index.html находится в корне output директории

### Проблема: CORS ошибки
- Anthropic API должен работать из браузера
- Если нет - создайте прокси через Cloudflare Workers или Vercel Edge Functions

### Проблема: Большой размер bundle (282 KB)
- Это нормально для Kotlin/JS приложения
- Можно оптимизировать через code splitting (advanced)

---

## 🎉 Готово!

После деплоя ваш AI чат будет доступен на **https://defendend.dev**

Каждый push в main будет автоматически деплоить новую версию.
